
import { formatCurrency } from 'online-marketplace/helpers/format-currency';
import { module, test } from 'qunit';

module('Unit | Helper | format twodecimal');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatCurrency([42]);
  assert.ok(result);
});

